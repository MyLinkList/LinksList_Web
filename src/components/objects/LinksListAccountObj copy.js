export const LinksListAccountObj = function(accoutImageIn, accountNameIn, linksIn, walletAddressIn) {
    const accoutImage = accoutImageIn;
    const accountName = accountNameIn;
    const links = linksIn;

    const walletAddress = walletAddressIn
    return { accoutImage, accountName, links, walletAddress };
};